# MyMedia Log 要件定義書

## アプリ概要

本・動画・記事など複数サービスにまたがるコンテンツを一元管理するパーソナルメディアトラッカー。
「あとで観る/読む」リストと「観た/読んだ」記録をひとつの場所にまとめることで、サービスをまたいだウォッチリスト・読みたいリストの管理を解決する。

---

## 技術スタック

| 項目 | 技術 |
|---|---|
| バックエンド | Ruby on Rails |
| フロントエンド | Tailwind CSS |
| データベース | PostgreSQL |
| 認証 | Devise |
| テスト | RSpec |
| デプロイ | Heroku |
| バージョン管理 | GitHub |

---

## 機能一覧

### 認証
- ユーザー登録・ログイン・ログアウト（Devise）

### コンテンツ管理（CRUD）
- コンテンツの新規登録（タイトル・URL・種別・メモ）
- 種別：本 / YouTube / 動画配信 / 記事 / その他
- 一覧表示・詳細表示・編集・削除

### ステータス管理
- `unread`（未消化）↔ `done`（消化済み）の切り替え（Railsのenumで実装）

### 絞り込み・検索
- 種別でのフィルタリング
- ステータスでのフィルタリング
- キーワード検索（タイトル）

### 記録
- 星評価（1〜5）※消化済み時のみ
- 一言メモ・感想

---

## 対象外（スコープ外）

- 他ユーザーとの共有・SNS機能
- 外部API連携（Google Books等）
- スマホアプリ
- アバター画像のアップロード

---

## 画面一覧

| 画面 | 説明 |
|---|---|
| トップ / ランディング | 未ログイン向け紹介ページ |
| ログイン / 新規登録 | Devise標準 |
| コンテンツ一覧 | フィルタ・検索付き |
| コンテンツ詳細 | メモ・評価・ステータス表示 |
| コンテンツ登録・編集 | フォーム |

---

## データモデル

### User（Deviseが生成）
| カラム | 型 | 説明 |
|---|---|---|
| email | string | メールアドレス |
| encrypted_password | string | パスワード（暗号化） |
| username | string | ユーザー名（必須・一意） |

### Content（コンテンツ）
| カラム | 型 | 説明 |
|---|---|---|
| id | integer | 主キー |
| user_id | integer | 外部キー（User） |
| title | string | タイトル（必須） |
| url | string | URL |
| media_type | string | 種別（book / youtube / video / article / other） |
| status | integer | ステータス（0: unread / 1: done）※ Railsのenumで実装すること |
| rating | integer | 星評価（1〜5、消化済み時のみ） |
| memo | text | メモ・感想 |
| created_at | datetime | 登録日時 |
| updated_at | datetime | 更新日時 |

---

## RSpecテスト方針

- **モデルスペック**：バリデーション・アソシエーション
- **リクエストスペック**：主要なCRUD操作
- **システムスペック**：ユーザー登録〜コンテンツ登録の主要フロー

---

## バリデーションルール

### User
| カラム | ルール |
|---|---|
| email | 必須・一意・メール形式 |
| password | 必須・6文字以上（Devise標準） |
| username | 必須・一意・2〜20文字 |

### Content
| カラム | ルール |
|---|---|
| title | 必須・255文字以内 |
| url | 任意・URL形式（入力された場合のみチェック） |
| media_type | 必須・指定の値のみ（book / youtube / video / article / other） |
| status | 必須・unread または done（enumで定義） |
| rating | 任意・1〜5の整数（消化済み時のみ入力可） |
| memo | 任意 |

---

## 認可ルール

- コンテンツの編集・削除は**登録したユーザー本人のみ**可能
- 他ユーザーのコンテンツへのアクセスは不可（404または403を返す）
- 未ログインユーザーはコンテンツ一覧・詳細を含む全ページにアクセス不可（ログインページへリダイレクト）


